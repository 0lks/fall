using UnityEngine;

// Internal dependencies
using FALL.Items.Weapons;
using FALL.Core;

public class PlayerManager : MonoBehaviour
{
    /*
    public Weapon wieldedWeapon;
    [HideInInspector] public Animator playerAnimator;

    private void Start()
    {
        playerAnimator = GameControl.player.GetComponent<Animator>();
        wieldedWeapon = FindObjectOfType<Bow>();

        if (wieldedWeapon != null)
        {
            // TODO: This is assuming that there is a bow and only the bow. Make this more adaptable later.
            wieldedWeapon.gameObject.GetComponent<Bow>().EquipTo(GameControl.player);
        }
    }

    public void EquipWeapon(Weapon weapon)
    {

    }
    */
}
